<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.CommentMapper">

    <resultMap id="CommentVOMap" type="com.travel.vo.CommentVO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="nickname" property="nickname"/>
        <result column="scenic_id" property="scenicId"/>
        <result column="scenic_name" property="scenicName"/>
        <result column="content" property="content"/>
        <result column="rating" property="rating"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectCommentPage" resultMap="CommentVOMap">
        SELECT
        c.id,
        c.user_id,
        u.nickname,
        c.scenic_id,
        s.name AS scenic_name,
        c.content,
        c.rating,
        c.create_time
        FROM comment c
        LEFT JOIN user u ON u.id = c.user_id AND u.is_deleted = 0
        LEFT JOIN scenic_spot s ON s.id = c.scenic_id AND s.is_deleted = 0
        WHERE c.is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND c.content LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY c.create_time DESC
    </select>

</mapper>
