<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.travel.mapper.UserMapper">

    <select id="pageQueryVO" resultType="com.travel.vo.UserVO">
        SELECT
        id, username, nickname, avatar, phone, gender, status, create_time
        FROM user
        <where>
            is_deleted = 0
            <if test="dto.keyword != null and dto.keyword != ''">
                AND (username LIKE CONCAT('%', #{dto.keyword}, '%')
                OR nickname LIKE CONCAT('%', #{dto.keyword}, '%'))
            </if>
            <if test="dto.status != null">
                AND status = #{dto.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
